name: Publish to WinGet

on:
  workflow_dispatch:
  release:
    types: [released]

jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: vedantmgoyal9/winget-releaser@v2
        with:
          identifier: StarLink.StarLink
          token: ${{ secrets.WINGET_TOKEN }}
